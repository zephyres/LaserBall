package world;

import org.newdawn.slick.Color;
import org.newdawn.slick.SlickException;
import utility.ReferenceFrame;
import utility.Type;
import entity.BlockStructure;
import entity.Grid;
import entity.Player;

public class Game extends World {
	ReferenceFrame rf;
	Player player;
	Grid grid;

	@Override
	public void init() throws SlickException {
		player = new Player();
		grid = new Grid();
		rf = new ReferenceFrame(player, 1280, 720);
		
		addObject(grid, 960, 540);
		addObject(player, 200f, 200f);
		addObject(Type.SMALL_BLOCK, new BlockStructure("src/text/schem1.txt"), 0, 0);
		
		setReferenceFrame(rf);
		setBackgroundColor(new Color(16, 30, 45));
	}
	
	@Override
	public int getID() {
		return 3;
	}
}
